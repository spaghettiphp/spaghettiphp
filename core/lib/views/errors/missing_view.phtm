<h1>A view <?php echo $details["view"] ?> não foi encontrada.</h1>
<p>O arquivo <code>app/views/<?php echo Inflector::underscore($details['controller']) ?>/<?php echo "{$details['view']}.p{$details['extension']}" ?></code> não pôde ser encontrado. Verifique se ele existe, e se possui seu método correspondente no controller.</p>